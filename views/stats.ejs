<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>URL Statistics</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="container mt-5">
    <div class="row">
      <div class="col-md-8 mx-auto">
        <div class="card">
          <div class="card-header bg-primary text-white">
            <h1 class="text-center">URL Statistics</h1>
          </div>
          <div class="card-body">
            <div class="mb-3">
              <h5>Original URL:</h5>
              <p><a href="<%= url.longUrl %>" target="_blank"><%= url.longUrl %></a></p>
            </div>
            
            <div class="mb-3">
              <h5>Short URL:</h5>
              <% if (url.customCode) { %>
                <p><a href="/<%= url.customCode %>" target="_blank"><%= url.customCode %></a></p>
              <% } else { %>
                <p><a href="/<%= url.shortCode %>" target="_blank"><%= url.shortCode %></a></p>
              <% } %>
            </div>
            
            <div class="mb-3">
              <h5>Total Clicks:</h5>
              <p><%= url.clicks %></p>
            </div>
            
            <div class="mb-3">
              <h5>Created At:</h5>
              <p><%= new Date(url.createdAt).toLocaleString() %></p>
            </div>
            
            <a href="/" class="btn btn-primary">Back to Home</a>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
